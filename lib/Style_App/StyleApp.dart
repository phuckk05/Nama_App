import 'package:flutter/material.dart';

class AppStyle {
  // Màu sắc chung
  static const Color primaryColor = Color(0xFFFF6F00); // Màu cam đậm
  static const Color secondaryColor = Color(0xFFFFCC80); // Màu cam nhạt
  static const Color backgroundColor = Color.fromARGB(227, 245, 245, 245); // Màu nền chung
  static const Color textColor = Color(0xFF333333); // Màu chữ chính
  static const Color textGreenColor = Colors.green; // Màu chữ phụ
  static const Color borderColor = Color(0xFFDDDDDD); // Màu viền
  static const Color successColor = Color(0xFF4CAF50); // Màu thành công
  static const Color errorColor = Color(0xFFF44336); // Màu lỗi

  // Kích thước chữ
  static const double textSizeSmall = 12.0;
  static const double textSizeMedium = 16.0;
  static const double textSizeLarge = 20.0;
  static const double textSizeTitle = 24.0;
  static const double textSizeHeader = 30.0;

  // Khoảng cách
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;

  // Độ bo góc
  static const double borderRadius = 8.0;

  //kieu chu
 static const String fontFamily = 'Times New Roman'; 

}